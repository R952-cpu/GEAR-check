# Dépendances
node_modules/

# Données de l'application — ne doivent JAMAIS être publiées.
# Elles contiennent les projets, checklists, photos et comptes-rendus réels.
data/
backups/
*.db
*.db-shm
*.db-wal
photos/

# Configuration locale de la machine : chemins disque, adresses réseau, port.
# Ce sont vos copies des modèles « .example » — elles restent chez vous.
.env
docker-compose.yml

# Divers
.DS_Store
*.log
