{
  "name": "regie",
  "version": "2.0.0",
  "description": "Gear Check — assistant de prépa caméra",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node tests/referentiel.test.js",
    "check": "node --check server.js && for f in src/*.js src/routes/*.js; do node --check \"$f\" || exit 1; done && echo 'syntaxe OK'"
  },
  "dependencies": {
    "compression": "^1.7.5",
    "express": "^4.21.2",
    "multer": "^2.0.2",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "jspdf": "^4.2.1"
  },
  "overrides": {
    "body-parser": "^1.20.6"
  }
}
